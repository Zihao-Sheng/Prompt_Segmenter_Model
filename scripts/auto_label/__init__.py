"""Command-line stages and utilities for the auto-label workflow.

The scripts in this package are intentionally runnable as standalone files from
the repository root. They cover frame extraction, proposal generation,
embedding/clustering, cluster review export, teacher memory updates, cleaned
pseudo-label export, and YOLO11-seg training.
"""
